## Packages
recharts | For all dashboard charts (line, bar, pie, etc.)
framer-motion | For smooth page transitions and micro-interactions
date-fns | For date formatting
clsx | For conditional class names
tailwind-merge | For merging tailwind classes safely

## Notes
Integration with /api/menu, /api/orders, /api/staff endpoints
Using mock data for charts where backend aggregation logic might be missing
Auth is mocked on client-side via AuthContext for demo purposes
